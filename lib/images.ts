/**
 * Image processing stub — Sharp pipeline lands in Phase 3.
 */
export async function optimizeImageStub(_buffer: Buffer) {
  return {
    ok: false as const,
    message: "Sharp image pipeline not implemented yet (Phase 3).",
  };
}
